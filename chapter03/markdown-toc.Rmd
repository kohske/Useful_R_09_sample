# 目次付きHTMLレポート

## 平均とは
\(\frac{1}{n}\sum_{i}x_i\)ということです。例えば1から10までの平均は`r mean(1:10)`です。

## 乱数の生成
### 正規乱数の平均
10000個の正規乱数の平均値は`r set.seed(42); mean(rnorm(10000))`でした。

### 一様乱数の平均
10000個の一様乱数の平均値は`r set.seed(42); mean(runif(10000))`でした。